name: draymon

services:
  draymon:
    build: .
    container_name: draymon
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - DB_PATH=/data/netmon.db
    volumes:
      - draymon_data:/data

volumes:
  draymon_data:
